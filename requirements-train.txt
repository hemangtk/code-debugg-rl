-r requirements.txt
# Note: torch is intentionally NOT pinned — the pytorch/pytorch base image
# already provides it. Pinning it here invites torchvision mismatch (one
# gets upgraded, the other doesn't). The script reinstalls torchvision
# post-install to keep them in sync.
transformers>=4.45.0
accelerate>=0.34.0
datasets>=3.0.0
peft>=0.13.0
trl==0.29.0
bitsandbytes>=0.44.0
unsloth>=2025.4.1
huggingface_hub>=0.27.0
